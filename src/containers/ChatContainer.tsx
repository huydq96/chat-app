import React from "react";
import ChatTemplate from "components/organisms/ChatTemplate";

const ChatTemplateContainer: React.FC = () => {
  return <ChatTemplate />;
};

export default ChatTemplateContainer;
